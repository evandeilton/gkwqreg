% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gkwqreg-methods.R
\name{coef.gkwqreg}
\alias{coef.gkwqreg}
\alias{logLik.gkwqreg}
\alias{nobs.gkwqreg}
\alias{AIC.gkwqreg}
\alias{BIC.gkwqreg}
\alias{family.gkwqreg}
\alias{formula.gkwqreg}
\alias{terms.gkwqreg}
\alias{model.frame.gkwqreg}
\alias{model.matrix.gkwqreg}
\alias{getCall.gkwqreg}
\alias{update.gkwqreg}
\alias{summary.gkwqreg}
\title{Extractor methods for a quantile regression fit}
\usage{
\method{coef}{gkwqreg}(object, part = NULL, ...)

\method{logLik}{gkwqreg}(object, ...)

\method{nobs}{gkwqreg}(object, ...)

\method{AIC}{gkwqreg}(object, ..., k = 2)

\method{BIC}{gkwqreg}(object, ...)

\method{family}{gkwqreg}(object, ...)

\method{formula}{gkwqreg}(x, ...)

\method{terms}{gkwqreg}(x, part = "mu", ...)

\method{model.frame}{gkwqreg}(formula, ...)

\method{model.matrix}{gkwqreg}(object, part = "mu", ...)

\method{getCall}{gkwqreg}(x, ...)

\method{update}{gkwqreg}(object, formula., ..., evaluate = TRUE)

\method{summary}{gkwqreg}(object, level = 0.95, vcov_type = NULL, ...)
}
\arguments{
\item{object, x, formula}{A \code{"gkwqreg"} fit, as returned by \code{\link[=gkwqreg]{gkwqreg()}}. The
three names correspond to the argument names of the respective generics.}

\item{part}{Which model part to extract, named as in \code{\link[=gkwq_parts]{gkwq_parts()}}:
\code{"mu"} for the conditional quantile, then the family's remaining
parameters. Partial matching applies. \code{coef()} accepts a vector of parts
and returns a named list in that case, and \code{part = NULL} (its default)
returns the whole stacked coefficient vector. \code{terms()} and
\code{model.matrix()} take exactly one part and default to \code{"mu"}.}

\item{...}{For \code{update()}, further named arguments replacing those of the
original call. Unused by the other methods.}

\item{k}{The penalty per parameter in \code{AIC()}. The default \code{k = 2} gives the
usual Akaike criterion. \code{k = log(nobs(object))} reproduces \code{BIC()} for an
unweighted fit; with prior weights the BIC penalty uses the effective
sample size \code{sum(w)} rather than the row count, because the weights
multiply the log-density and a weighted fit carries that much evidence.}

\item{formula.}{A formula update specification for \code{update()}, in the sense
of \code{\link[stats:update.formula]{stats::update.formula()}}, applied to the multi-part model formula.}

\item{evaluate}{If \code{TRUE} (the default) \code{update()} evaluates the modified
call and returns the new fit; if \code{FALSE} it returns the unevaluated call,
which is useful for inspection or for deferred evaluation.}

\item{level}{Confidence level stored on the \code{summary()} object for a caller
that wants it. The printed table reports estimates, standard errors and
p-values rather than an interval, so changing \code{level} does not change what
\code{print()} shows; use \code{\link[=confint]{confint()}} for an interval at a chosen level.}

\item{vcov_type}{Which covariance estimator \code{summary()} should use for its
standard errors, one of \code{"expected"}, \code{"observed"} or \code{"sandwich"}; see
\code{\link[=vcov.gkwqreg]{vcov.gkwqreg()}}. \code{NULL}, the default, uses the type recorded in the fit's
control object. If the requested type cannot be computed, \code{summary()} falls
back to the stored matrix rather than failing.}
}
\value{
\itemize{
\item \code{coef()}: a named numeric vector. The whole stacked vector, with names
\code{"part:term"}, when \code{part} is \code{NULL}; a single part's coefficients, with
bare term names, when one part is given; a named list of such vectors when
several are given.
\item \code{logLik()}: an object of class \code{"logLik"} with attributes \code{df}
(the number of estimated coefficients) and \code{nobs}.
\item \code{nobs()}: a single integer, the number of observations used in the fit.
\item \code{AIC()}, \code{BIC()}: a single number.
\item \code{family()}: an object of class \code{"gkwq_family"}, a list with components
\code{family}, \code{anchor}, \code{tau}, \code{parts} and \code{link}, with a \code{print} method.
\item \code{formula()}: the multi-part model formula as supplied.
\item \code{terms()}: the \code{"terms"} object of one part.
\item \code{model.frame()}: the stored model frame.
\item \code{model.matrix()}: the design matrix of one part, with an \code{"assign"}
attribute and contrasts as recorded at fit time.
\item \code{getCall()}: the matched call that produced the fit.
\item \code{update()}: a new \code{"gkwqreg"} fit, or the unevaluated call when
\code{evaluate = FALSE}.
\item \code{summary()}: an object of class \code{"summary.gkwqreg"} carrying the
coefficient table, the fit statistics, the pinball loss, the pseudo-R1, the
empirical coverage and the information condition number, with a \code{print}
method.
}
}
\description{
The standard model accessors, adapted to a model whose coefficients are split
across several parts. \code{coef()}, \code{terms()} and \code{model.matrix()} therefore take
a \code{part} argument; the rest behave as their generics lead one to expect.
}
\details{
A \code{"gkwqreg"} object carries one linear predictor per modelled parameter. The
first part is always \code{mu}, the conditional \eqn{\tau}-quantile; the remaining
parts are the family's free parameters with the anchored one removed, as
reported by \code{\link[=gkwq_parts]{gkwq_parts()}}. Coefficients are stored in a single stacked
vector whose names have the form \code{"part:term"}, so \code{coef(object)} returns
everything at once and \code{coef(object, part = "mu")} returns just the quantile
block with its bare term names.

\code{logLik()}, \code{AIC()} and \code{BIC()} describe the model at \strong{one} quantile level.
They are comparable across families, anchors and covariate sets fitted at the
same \code{tau}, and are not comparable across levels: different levels answer
different questions of the same data and their likelihoods are not on a
common scale.

\code{family()} returns a small description object rather than a
\code{\link[stats:family]{stats::family()}} object, because a Generalized Kumaraswamy quantile model has
no single variance function or canonical link to report. It records the
family, the anchor, the quantile level, the part names and the link used by
each part, and prints them compactly.

Some accessors depend on what the fit was asked to retain.
\code{model.frame()} requires \code{model = TRUE} (the default) and errors otherwise;
\code{model.matrix()} uses the stored design matrices when the fit was made with
\code{x = TRUE}, and otherwise rebuilds them from the model frame, so it needs one
or the other. Each error message names the argument to set on the refit.
}
\section{What \code{update()} does and does not carry over}{

\code{update()} modifies the recorded call and re-evaluates it, so any argument of
\code{\link[=gkwqreg]{gkwqreg()}} can be replaced by name -- \code{tau}, \code{family}, \code{anchor}, \code{data},
\code{control} and so on. Changing \code{tau} refits at the new level, which is a
different model and not the same thing as reading another level off the
present fit; see \code{\link[=predict.gkwqreg]{predict.gkwqreg()}} for that distinction. Changing \code{family}
or \code{anchor} may change the number of formula parts the model expects, in
which case the formula must be updated in the same call.
}

\examples{
set.seed(2024)
n   <- 300
x1  <- runif(n, -2, 2)
x2  <- rbinom(n, 1, 0.5)
mu  <- plogis(0.3 + 0.9 * x1 - 0.5 * x2)
bt  <- log1p(-0.5) / log1p(-mu^2)
y   <- gkwq_quantile(runif(n), alpha = 2, beta = bt)
dat <- data.frame(y = y, x1 = x1, x2 = factor(x2))

fit <- gkwqreg(y ~ x1 + x2 | x1, data = dat, tau = 0.5, family = "kw")

## -- coefficients, whole and by part -------------------------------------
round(coef(fit), 4)                 # names are "part:term"
round(coef(fit, part = "mu"), 4)    # just the conditional-median equation
names(coef(fit, part = c("mu", "alpha")))

## -- what the model is -----------------------------------------------------
family(fit)
gkwq_parts("kw")                    # the part contract this fit obeys
formula(fit)

## -- fit statistics, all at this one quantile level ----------------------
c(logLik = as.numeric(logLik(fit)), df = attr(logLik(fit), "df"),
  n = nobs(fit), AIC = AIC(fit), BIC = BIC(fit))
all.equal(AIC(fit, k = log(nobs(fit))), BIC(fit))   # TRUE

## -- design matrices differ by part --------------------------------------
fitx <- gkwqreg(y ~ x1 + x2 | x1, data = dat, tau = 0.5, family = "kw",
                x = TRUE)
colnames(model.matrix(fitx, part = "mu"))       # intercept, x1, x21
colnames(model.matrix(fitx, part = "alpha"))    # intercept, x1
## This is why sandwich::vcovHC() cannot work here: there is no single
## design matrix whose columns match the score matrix.

## -- summary, with a covariance estimator of your choosing ---------------
summary(fit, vcov_type = "sandwich")

## -- update() re-evaluates the call --------------------------------------
getCall(fit)
update(fit, . ~ x1 | x1, evaluate = FALSE)      # inspect before running
fit_simpler <- update(fit, . ~ x1 | x1)
c(full = AIC(fit), simpler = AIC(fit_simpler))
}
\seealso{
\code{\link[=gkwqreg]{gkwqreg()}} for the model, \code{\link[=gkwq_parts]{gkwq_parts()}} for the part contract,
\code{\link[=vcov.gkwqreg]{vcov.gkwqreg()}}, \code{\link[=confint.gkwqreg]{confint.gkwqreg()}}, \code{\link[=fitted.gkwqreg]{fitted.gkwqreg()}}.
}
