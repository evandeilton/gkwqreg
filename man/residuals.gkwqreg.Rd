% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gkwqreg-residuals.R
\name{residuals.gkwqreg}
\alias{residuals.gkwqreg}
\title{Residuals from a quantile regression fit}
\usage{
\method{residuals}{gkwqreg}(
  object,
  type = c("quantile", "cox-snell", "pearson", "deviance", "response", "check",
    "tau-sign"),
  ...
)
}
\arguments{
\item{object}{A \code{"gkwqreg"} fit.}

\item{type}{The residual to compute; see Details.}

\item{...}{Unused.}
}
\value{
A numeric vector of residuals.
}
\description{
Residuals from a quantile regression fit
}
\details{
\describe{
\item{\code{"quantile"}}{Dunn and Smyth (1996), \verb{qnorm(F(y_i; theta_i))}. Exact,
with no randomization: the family is continuous. Standard normal under
correct specification, and the default.}
\item{\code{"cox-snell"}}{\code{-log(1 - F(y_i))}; unit exponential under correct
specification.}
\item{\code{"pearson"}}{\code{(y_i - E[Y|x_i]) / sd(Y|x_i)}, both by quadrature.
Provided for comparability with mean-regression packages.}
\item{\code{"deviance"}}{\verb{sign(y_i - Q_i) sqrt(2 (l_i(y_i) - l_i(Q_i)))}, where
\code{l_i(y_i)} is the log-likelihood with the fitted quantile moved onto the
observation. For a continuous density the usual saturated likelihood is
unbounded, so the reference here is the quantile that would place \code{y_i}
exactly at level \code{tau} -- which is the quantity this model is about.}
\item{\code{"response"}}{\code{y_i - Q(tau|x_i)}. \strong{The reference point is the
quantile, not the mean}, unlike the same name in mean regression.}
\item{\code{"check"}}{\verb{(y_i - Q_i)(tau - 1\{y_i < Q_i\})}. Its mean is the pinball
loss; plotted against a covariate it shows where the fit deteriorates.}
\item{\code{"tau-sign"}}{\verb{1\{y_i <= Q_i\} - tau}. Under correct specification
\verb{E[1\{Y <= Q_tau(X)\} | X] = tau} exactly, so a non-zero mean within a
covariate bin is direct, distribution-free evidence of quantile
miscalibration -- a check that mean regression has no analogue for.}
}
}
\references{
Dunn, P. K. and Smyth, G. K. (1996). Randomized quantile residuals.
\emph{Journal of Computational and Graphical Statistics} \strong{5}, 236-244.
}
