% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gkwqreg-methods.R, R/gkwqreg-predict.R,
%   R/gkwqreg-residuals.R
\name{coef.gkwqregs}
\alias{coef.gkwqregs}
\alias{fitted.gkwqregs}
\alias{logLik.gkwqregs}
\alias{summary.gkwqregs}
\alias{predict.gkwqregs}
\alias{residuals.gkwqregs}
\title{Methods for a set of quantile regression fits}
\usage{
\method{coef}{gkwqregs}(object, ...)

\method{fitted}{gkwqregs}(object, ...)

\method{logLik}{gkwqregs}(object, ...)

\method{summary}{gkwqregs}(object, ...)

\method{predict}{gkwqregs}(object, newdata = NULL, ...)

\method{residuals}{gkwqregs}(object, ...)
}
\arguments{
\item{object}{A \code{"gkwqregs"} container, as returned by \code{\link[=gkwqreg]{gkwqreg()}} with a
vector-valued \code{tau}.}

\item{...}{Passed on to the corresponding method for each individual fit.}

\item{newdata}{Used by \code{predict()} only: an optional data frame in which to
evaluate every fit, with the same requirements as
\code{\link[=predict.gkwqreg]{predict.gkwqreg()}}. \code{NULL}, the default, uses the estimation data.}
}
\value{
\itemize{
\item \code{coef()}: a numeric matrix with one row per coefficient and one column per
quantile level, the columns named by level.
\item \code{fitted()}: a numeric matrix with one row per observation and one column
per level, holding each fit's conditional quantiles.
\item \code{summary()}: a list of \code{"summary.gkwqreg"} objects, one per level.
\item \code{predict()}: a numeric matrix with one row per prediction and one column
per level, from each fit in turn.
\item \code{residuals()}: a numeric matrix with one row per observation and one column
per level.
\item \code{logLik()}: never returns; it raises an error explaining why.
}
}
\description{
Accessors for the \code{"gkwqregs"} container returned by \code{\link[=gkwqreg]{gkwqreg()}} when \code{tau}
has length greater than one. The container holds one entirely independent fit
per quantile level in \code{object$fits}, and these methods arrange their output
with one column per level.
}
\details{
Levels are stored sorted and de-duplicated, and each fit is estimated on its
own. Nothing ties the levels together, which is what makes quantile crossing
possible and worth testing for; see \code{\link[=check_crossing]{check_crossing()}} and \code{\link[=rearrange]{rearrange()}}.

\code{logLik()} deliberately fails. The container holds one log-likelihood per
level, and those are neither comparable across levels nor additive: summing
them would treat the same observations as independent evidence several times
over, and comparing them would compare answers to different questions. Take
the likelihood of an individual fit instead, as the error message says.
}
\examples{
set.seed(2024)
n   <- 300
x1  <- runif(n, -2, 2)
mu  <- plogis(0.3 + 0.9 * x1)
bt  <- log1p(-0.5) / log1p(-mu^2)
y   <- gkwq_quantile(runif(n), alpha = 2, beta = bt)
dat <- data.frame(y = y, x1 = x1)

fits <- gkwqreg(y ~ x1, data = dat, tau = c(0.25, 0.5, 0.75),
                family = "kw")
fits

round(coef(fits), 4)
## One column per level. The mu:x1 row is the covariate effect on the log
## quantile odds at each level; its drift across columns is what a quantile
## regression is for.

Q <- fitted(fits)
dim(Q)
colnames(Q)                   # the levels, formatted: "0.25" "0.50" "0.75"
colMeans(dat$y <= Q)          # 0.25, 0.46, 0.73: coverage at each level

## Each level is a separate fit, so nothing forces the columns to be
## ordered; check rather than assume.
mean(Q[, 1] <= Q[, 2] & Q[, 2] <= Q[, 3])   # 1: no crossing in this sample
}
\seealso{
\code{\link[=gkwqreg]{gkwqreg()}}, \code{\link[=quantile_process]{quantile_process()}} for the quantile process on a
grid, \code{\link[=check_crossing]{check_crossing()}} for crossing diagnostics,
\code{\link[=predict.gkwqreg]{predict.gkwqreg()}} for reading several levels off a \emph{single} fit.
}
