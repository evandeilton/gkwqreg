% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gkwqreg-control.R
\name{gkwq_control}
\alias{gkwq_control}
\title{Control parameters for \code{\link[=gkwqreg]{gkwqreg()}}}
\usage{
gkwq_control(
  method = c("nlminb", "BFGS", "L-BFGS-B", "CG", "Nelder-Mead"),
  maxit = 1000L,
  reltol = 1e-10,
  start_method = c("auto", "ols", "intercept", "gkwdist", "user"),
  start = NULL,
  eps_y = 1e-10,
  eps_mu = 1e-08,
  tiny = 1e-12,
  vcov_type = c("expected", "observed", "sandwich"),
  hessian = TRUE,
  warm_start = TRUE,
  silent = TRUE,
  ...
)
}
\arguments{
\item{method}{Optimizer passed to \code{\link[stats:nlminb]{stats::nlminb()}} or \code{\link[stats:optim]{stats::optim()}}.
\code{"nlminb"} (the default) is a quasi-Newton method with the AD gradient.}

\item{maxit}{Maximum optimizer iterations.}

\item{reltol}{Relative convergence tolerance.}

\item{start_method}{How starting values are built. \code{"auto"} runs the full
algorithm with an intercept-only pre-fit for the harder families,
\code{"ols"} skips the pre-fit, \code{"intercept"} always runs it, \code{"gkwdist"} takes
marginal estimates and leaves every slope at zero, \code{"user"} requires
\code{start}.}

\item{start}{Named list of starting coefficients per part, used when
\code{start_method = "user"}.}

\item{eps_y}{Response values are clamped into \verb{[eps_y, 1 - eps_y]}. Values at
exactly 0 or 1 lie outside the family's support.}

\item{eps_mu}{Smooth clamp applied to the fitted conditional quantile inside
the tape. The anchor solves overflow in opposite tails (the beta solve as
\code{mu -> 0}, the alpha solve as \code{mu -> 1}; SPEC N6), so this bound is what
keeps either usable near the boundary.}

\item{tiny}{Floor for log-scale quantities that must stay strictly negative.}

\item{vcov_type}{Default covariance: \code{"expected"} from \code{sdreport},
\code{"observed"} from \code{optimHess} on the AD gradient, or \code{"sandwich"}.}

\item{hessian}{Whether to compute the covariance matrix at all.}

\item{warm_start}{For a vector \code{tau}, fit the level nearest the median first
and warm-start outward in both directions.}

\item{silent}{Suppress TMB's tracing output.}

\item{...}{Ignored, for forward compatibility.}
}
\value{
An object of class \code{"gkwq_control"}.
}
\description{
Control parameters for \code{\link[=gkwqreg]{gkwqreg()}}
}
\examples{
gkwq_control(maxit = 500, vcov_type = "sandwich")
}
